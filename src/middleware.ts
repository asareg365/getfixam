/**
 * This file is intentionally neutralized to resolve a conflict with src/proxy.ts.
 * Next.js environment requirements mandate using only one routing file.
 * All security and routing logic is now handled in src/proxy.ts.
 */
export const config = {
  matcher: [],
};

export default function neutralizedMiddleware() {
  return;
}
